{{-- Inline brand wordmark. Matches the header logo's type treatment but
     sizes itself to whatever text it sits inside — a paragraph, heading,
     or button. Use in rendered copy only: <x-brand/>. It renders HTML, so
     it can't go in plain-string places like the page-title directive or an
     alt attribute — keep those as the literal text MEGABIBLE dot net. --}}
<span class="mb-brand">MEGABIBLE<span class="mb-tld">.net</span></span>
